<UserControl x:Class="VrcGroupGuardian.Views.InstancesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="900">
    
    <UserControl.Resources>
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="MinWidth" Value="60"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        
        <Style x:Key="StatusIndicatorStyle" TargetType="Rectangle">
            <Setter Property="Width" Value="12"/>
            <Setter Property="Height" Value="12"/>
            <Setter Property="RadiusX" Value="6"/>
            <Setter Property="RadiusY" Value="6"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Control Panel -->
        <Border Grid.Row="0" 
                Background="#F8F9FA" 
                BorderBrush="#E9ECEF" 
                BorderThickness="1" 
                CornerRadius="4"
                Padding="15"
                Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button Content="{Binding MonitoringButtonText}" 
                            Command="{Binding ToggleMonitoringCommand}"
                            Background="{Binding MonitoringButtonColor}"
                            Foreground="White"
                            Padding="15,8"
                            Margin="0,0,10,0"/>
                    
                    <Button Content="{Binding EnforcementButtonText}" 
                            Command="{Binding ToggleEnforcementCommand}"
                            Background="{Binding EnforcementButtonColor}"
                            Foreground="White"
                            Padding="15,8"
                            Margin="0,0,10,0"/>
                    
                    <TextBlock Text="{Binding StatusText}" 
                             VerticalAlignment="Center"
                             Foreground="#666"
                             FontSize="12"
                             Margin="10,0"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="🔄 Refresh" 
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource ActionButtonStyle}"/>
                    <Button Content="📊 Export" 
                            Command="{Binding ExportCommand}"
                            Style="{StaticResource ActionButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Policy Configuration -->
        <Expander Grid.Row="1" 
                  Header="Policy Configuration" 
                  IsExpanded="False"
                  Margin="0,0,0,15">
            <Border Background="#F8F9FA" 
                    BorderBrush="#E9ECEF" 
                    BorderThickness="1" 
                    CornerRadius="4"
                    Padding="15"
                    Margin="0,5,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Column="0" Grid.Row="0" Margin="0,0,10,10">
                        <Label Content="Grace Period (seconds):"/>
                        <TextBox Text="{Binding GracePeriodSeconds, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="100" HorizontalAlignment="Left"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="1" Grid.Row="0" Margin="0,0,10,10">
                        <Label Content="Polling Interval (seconds):"/>
                        <TextBox Text="{Binding PollingIntervalSeconds, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="100" HorizontalAlignment="Left"/>
                    </StackPanel>
                    
                    <StackPanel Grid.Column="2" Grid.Row="0" Margin="0,0,0,10">
                        <Label Content="Rate Limit (req/min):"/>
                        <TextBox Text="{Binding RateLimitRequestsPerMinute, UpdateSourceTrigger=PropertyChanged}" 
                                 Width="100" HorizontalAlignment="Left"/>
                    </StackPanel>
                    
                    <CheckBox Grid.Column="0" Grid.Row="1" 
                              Content="Enable Notifications" 
                              IsChecked="{Binding NotificationsEnabled}" 
                              Margin="0,5"/>
                    
                    <CheckBox Grid.Column="1" Grid.Row="1" 
                              Content="Export Audit Logs" 
                              IsChecked="{Binding ExportAuditLogs}" 
                              Margin="0,5"/>
                    
                    <Button Grid.Column="2" Grid.Row="2" 
                            Content="Save Policy" 
                            Command="{Binding SavePolicyCommand}"
                            HorizontalAlignment="Right"
                            Padding="15,8"
                            Background="#28A745"
                            Foreground="White"
                            Margin="0,10,0,0"/>
                </Grid>
            </Border>
        </Expander>

        <!-- Instances Data Grid -->
        <DataGrid Grid.Row="2" 
                  x:Name="InstancesDataGrid"
                  ItemsSource="{Binding Instances}"
                  SelectedItem="{Binding SelectedInstance}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  RowHeaderWidth="0"
                  AlternatingRowBackground="#F8F9FA"
                  BorderBrush="#E9ECEF"
                  BorderThickness="1"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.IsContainerVirtualizable="True">
            
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Status" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Rectangle Style="{StaticResource StatusIndicatorStyle}"
                                     Fill="{Binding Status, Converter={StaticResource StatusToColorConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <DataGridTextColumn Header="World Name" 
                                    Binding="{Binding WorldName}" 
                                    Width="200"/>
                
                <DataGridTextColumn Header="Instance Type" 
                                    Binding="{Binding InstanceType}" 
                                    Width="100"/>
                
                <DataGridTextColumn Header="Users" 
                                    Width="80">
                    <DataGridTextColumn.Binding>
                        <MultiBinding StringFormat="{}{0}/{1}">
                            <Binding Path="UserCount"/>
                            <Binding Path="MaxUsers"/>
                        </MultiBinding>
                    </DataGridTextColumn.Binding>
                </DataGridTextColumn>
                
                <DataGridTextColumn Header="Region" 
                                    Binding="{Binding Region}" 
                                    Width="80"/>
                
                <DataGridCheckBoxColumn Header="Age Gated" 
                                        Binding="{Binding AgeGated}" 
                                        Width="80"/>
                
                <DataGridTextColumn Header="Created" 
                                    Binding="{Binding CreatedAt, StringFormat=\{0:MM/dd HH:mm\}}" 
                                    Width="100"/>
                
                <DataGridTextColumn Header="Countdown" 
                                    Binding="{Binding CountdownTimer, Converter={StaticResource TimeSpanToMinutesConverter}}" 
                                    Width="80"/>
                
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Close" 
                                        Command="{Binding DataContext.CloseInstanceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#DC3545"
                                        Foreground="White"/>
                                <Button Content="Cancel" 
                                        Command="{Binding DataContext.CancelClosureCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ActionButtonStyle}"
                                        Background="#FFC107"
                                        Foreground="Black"
                                        Visibility="{Binding Status, Converter={StaticResource StatusToVisibilityConverter}}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Statistics Bar -->
        <Border Grid.Row="3" 
                Background="#F8F9FA" 
                BorderBrush="#E9ECEF" 
                BorderThickness="1" 
                CornerRadius="4"
                Padding="15"
                Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding TotalInstances}" 
                             FontSize="18" 
                             FontWeight="Bold" 
                             HorizontalAlignment="Center"/>
                    <TextBlock Text="Total Instances" 
                             FontSize="11" 
                             Foreground="#666" 
                             HorizontalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding ActiveInstances}" 
                             FontSize="18" 
                             FontWeight="Bold" 
                             HorizontalAlignment="Center"
                             Foreground="#28A745"/>
                    <TextBlock Text="Active" 
                             FontSize="11" 
                             Foreground="#666" 
                             HorizontalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding FlaggedInstances}" 
                             FontSize="18" 
                             FontWeight="Bold" 
                             HorizontalAlignment="Center"
                             Foreground="#FFC107"/>
                    <TextBlock Text="Flagged" 
                             FontSize="11" 
                             Foreground="#666" 
                             HorizontalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="3" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding ClosingInstances}" 
                             FontSize="18" 
                             FontWeight="Bold" 
                             HorizontalAlignment="Center"
                             Foreground="#DC3545"/>
                    <TextBlock Text="Closing" 
                             FontSize="11" 
                             Foreground="#666" 
                             HorizontalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="4" HorizontalAlignment="Center">
                    <TextBlock Text="{Binding TotalUsers}" 
                             FontSize="18" 
                             FontWeight="Bold" 
                             HorizontalAlignment="Center"
                             Foreground="#007BFF"/>
                    <TextBlock Text="Total Users" 
                             FontSize="11" 
                             Foreground="#666" 
                             HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>